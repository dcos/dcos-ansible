---
- import_tasks: main-Linux.yml
  become: true
  when:
    - not ansible_os_family == "Windows"

- fail:
    msg: Only Linux nodes are allowed as Bootstrap nodes
  when: ansible_os_family == "Windows"
